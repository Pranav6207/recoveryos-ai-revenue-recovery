import Link from "next/link";

import { LaunchWorkspaceButton } from "@/components/launch-workspace-button";
import { kindLabels } from "@/lib/recovery-data";
import { recoveryKinds } from "@/lib/recovery-types";

export default function Home() {
  return <main className="min-h-screen bg-slate-950 text-white"><section className="mx-auto max-w-7xl px-6 py-8"><nav className="flex items-center justify-between"><p className="text-lg font-black">Recovery<span className="text-blue-400">OS</span></p><Link className="text-sm font-bold text-slate-300 hover:text-white" href="/verification">Integration status</Link></nav><div className="grid gap-12 py-20 lg:grid-cols-[1.2fr_.8fr] lg:items-center"><div><p className="text-sm font-bold uppercase tracking-[0.2em] text-blue-300">Razorpay Buildathon · Test Mode</p><h1 className="mt-5 max-w-3xl text-5xl font-black leading-[1.03] tracking-tight sm:text-6xl">Turn at-risk revenue into auditable recovery.</h1><p className="mt-7 max-w-2xl text-lg leading-8 text-slate-300">A judge-friendly recovery workspace that detects payment risk, chooses a compliant next step, and proves the outcome across seven playbooks—without needing an account or real customer data.</p><div className="mt-9 flex flex-wrap items-center gap-4"><LaunchWorkspaceButton /><p className="text-sm text-slate-400">Private 24-hour synthetic run · zero configuration</p></div></div><aside className="rounded-3xl border border-slate-700 bg-slate-900 p-7 shadow-2xl"><p className="text-sm font-bold text-blue-300">Recovery loop</p><ol className="mt-5 space-y-4 text-sm"><li><b>01</b> <span className="ml-3 text-slate-300">Risk signal from Test Checkout or a simulator</span></li><li><b>02</b> <span className="ml-3 text-slate-300">Gemini diagnosis with deterministic fallback</span></li><li><b>03</b> <span className="ml-3 text-slate-300">Consent, caps, quiet-hours and promise policy gate</span></li><li><b>04</b> <span className="ml-3 text-slate-300">Bounded action, provider evidence, audit and metrics</span></li></ol></aside></div></section><section className="border-y border-slate-800 bg-slate-900/60"><div className="mx-auto max-w-7xl px-6 py-12"><p className="text-sm font-bold uppercase tracking-[0.18em] text-blue-300">Seven recovery playbooks</p><div className="mt-6 grid gap-4 md:grid-cols-2 xl:grid-cols-4">{recoveryKinds.map((kind, index) => <Link key={kind} href={`/playbooks/${kind}`} className="rounded-2xl border border-slate-700 bg-slate-900 p-5 transition hover:-translate-y-0.5 hover:border-blue-400"><p className="text-xs font-bold text-blue-300">0{index + 1}</p><h2 className="mt-3 font-bold text-white">{kindLabels[kind]}</h2><p className="mt-2 text-sm leading-6 text-slate-400">Inspect the recovery policy, safe action and evidence model.</p></Link>)}</div></div></section><section className="mx-auto grid max-w-7xl gap-6 px-6 py-16 md:grid-cols-3"><div><h2 className="text-xl font-bold">Honest Test Mode</h2><p className="mt-3 text-sm leading-6 text-slate-400">Razorpay uses only Test Mode. A payment counts only after a verified webhook—not after a click.</p></div><div><h2 className="text-xl font-bold">Safe by default</h2><p className="mt-3 text-sm leading-6 text-slate-400">No real customers, no judge emails, no real calls, no customer-supplied credentials, and policy blocks are visible.</p></div><div><h2 className="text-xl font-bold">Failure resilient</h2><p className="mt-3 text-sm leading-6 text-slate-400">Every live adapter has a labeled fallback, so a provider outage cannot prevent evaluation.</p></div></section></main>;
}
